ontocalc化にある、ontology計算DSLを動作するように完成させて、DSLをつかった簡単なエディタかつ実行評価環境を作ってほしい。

related_documents/the_ontocalc_summary.md　には背景となる情報がある
ontcalc/README.md　には、ontocalに含まれるファイルの説明がある。

## 要件
- この理論に加えて、オントロジーAからオントロジーBへの変換(transfer)演算を追加する
- DSLの実行は、仕様にあるような複雑なライブラリを使わなくても、例えば、パーサ、中間言語への展開のあとは、その具体的な評価は、AI APIを呼ぶことでこれを代替して実現してもよい。

これを、少なくとも2つの事例をつけて、テストすること。
事例１：オントロジー操作を直感的に理解できるトイ例
事例２：工場の生産工程とGHGレポートの事例は、